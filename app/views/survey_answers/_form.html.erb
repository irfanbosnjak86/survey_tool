<%= form_with(model: [survey, survey_answer]) do |form| %>
  <% if survey_answer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(survey_answer.errors.count, "error") %> prohibited this survey_answer from being saved:</h2>

      <ul>
        <% survey_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <p class="md-5">
      Question: <%= form.label survey.question.text %>
    </p>

    <p class="p-4">Yes/No <%= form.check_box :answer %></p>

  </div>

  <div class="my-5">
    <%= form.submit class: "rounded-lg py-1 px-3 bg-blue-500 text-black block font-xs" %>
  </div>
<% end %>
