<p style="color: green"><%= notice %></p>

<%= render @survey_answer %>

<div>
  <%= link_to "Edit this survey answer", edit_survey_survey_answer_path(@survey, @survey_answer) %> |
  <%= link_to "Back to survey answers", survey_survey_answers_path(@survey) %>

  <%= button_to "Destroy this survey answer", [@survey, @survey_answer], method: :delete %>
</div>
