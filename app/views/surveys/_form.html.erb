<%= form_with(model: survey) do |form| %>
  <% if survey.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
        <% survey.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div class="my-5 py-3">
    <%= form.fields_for :question do |question| %>
      <%= question.label "Question", style: "display: block" %>
      <%= question.text_field :text %>
    <% end %>
  </div>

  <div>
    <%= form.submit id: "submit", class: "rounded-lg py-2 px-5 bg-blue-500 text-black block font-xs" %>
  </div>
<% end %>
